# Slide 6 Data Persistence Prompt

Create a 16:9 Chinese data architecture slide titled "数据沉淀支撑追溯、统计与迭代". This page is a technical supplement that should be understandable at a glance.

Use a grouped table or layered storage map. Include these data groups:
- "用户与权限": users, roles, permissions, user_roles
- "医患关系与病例": patient_profiles, doctor_patient_relations, medical_records
- "检测业务": detection_tasks, detection_results, cxr_images
- "对话与报告": chat_sessions, chat_messages, detection_reports
- "模型训练": training_tasks, training_metrics, model_versions
- "Agent 扩展": agent_registry, agent_invocations, agent_teams

Show one central message: "每次检测、每段对话、每份病例、每个模型版本都可追溯". Add three outcome tags on the right: "历史回溯", "统计看板", "模型迭代".

Style: professional database diagram, not a raw ER diagram. Use small table icons, database cylinders, or clean grouped cards. Keep text large enough for presentation viewing. Avoid showing all columns or detailed schema relationships.
